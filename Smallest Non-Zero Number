class Solution {
  public:
    int find(vector<int>& arr) {
        // code here
        int ans = 0, n = arr.size(), xAfter = 0;
        
        for (int i = n-1; i >= 0; i--) {
            xAfter = ceil((((double)xAfter + arr[i])/2));
        }
        
        return xAfter;
    }
};
